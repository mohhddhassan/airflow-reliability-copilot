# Airflow itself is pinned via the base image in Dockerfile
# (apache/airflow:3.1.5-python3.12), so it is intentionally NOT listed here.

apache-airflow-providers-standard>=1.6.0
apache-airflow-providers-common-ai[google]>=0.5.0
apache-airflow-providers-common-io
clickhouse-connect>=0.8.0
requests>=2.31.0

# The [google] extra above pulls in the Gemini SDK pydantic-ai needs.
# Swap/add [anthropic] or [openai] here if you switch providers -- see
# .env.example.

# Testing
pytest>=8.0.0
